# Bug Log - Детальний звіт про помилки в папці /src

## Дата створення: 2025-01-XX

---

## 🔴 КРИТИЧНІ ПОМИЛКИ

### 1. **src/firebase.js** - Відсутність перевірки змінних середовища
**Рядки:** 5-27 (виправлено)  
**Тип:** Критична помилка ініціалізації  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Файл використовує змінні середовища без перевірки на `undefined` або `null`. Якщо `.env` файл відсутній або змінні не встановлені, Firebase ініціалізується з `undefined` значеннями, що призведе до помилок під час роботи додатку.

**Наслідки:**
- Додаток може не запуститися
- Firebase операції будуть несправними
- Немає інформативного повідомлення про помилку

**Виправлення:**
Додано перевірку всіх необхідних змінних середовища перед ініціалізацією Firebase. Якщо якась змінна відсутня, викидається інформативна помилка зі списком відсутніх змінних та інструкціями для користувача.

**Приклад виправленого коду:**
```javascript
// Перевірка змінних середовища
const requiredEnvVars = {
    apiKey: 'REACT_APP_FIREBASE_API_KEY',
    // ... всі необхідні змінні
};

const missingVars = [];
for (const [key, envVar] of Object.entries(requiredEnvVars)) {
    if (!process.env[envVar]) {
        missingVars.push(envVar);
    }
}

if (missingVars.length > 0) {
    const errorMessage = `Помилка конфігурації Firebase: відсутні змінні середовища:\n${missingVars.join('\n')}\n\nПереконайтеся, що файл .env існує та містить всі необхідні змінні.\nПерегляньте .env.example для прикладу.`;
    console.error(errorMessage);
    throw new Error(errorMessage);
}
```

---

### 2. **src/api/cocktailApi.js** - Відсутність обробки null/undefined відповідей API
**Рядки:** 3-50 (виправлено)  
**Тип:** Помилка обробки даних  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Всі функції API можуть повернути `null` або `undefined`, якщо API повертає порожню відповідь або помилку, але код не перевіряє ці випадки перед доступом до властивостей.

**Наслідки:**
- Можливі помилки типу "Cannot read property 'drinks' of null"
- Додаток може крашитися при несправності API
- Немає fallback значень

**Виправлення:**
Додано обробку null/undefined для всіх функцій API:
- Всі функції тепер використовують optional chaining (`?.`) та повертають `null` як fallback значення
- У `src/pages/Cocktails.jsx` додано фільтрацію `null` значень з масиву деталей коктейлів
- Додано перевірки перед використанням результатів API

**Приклад виправленого коду:**
```javascript
export async function getCocktailByName(name) {
  const res = await fetch(`${API_BASE}/search.php?s=${encodeURIComponent(name)}`);
  if (!res.ok) throw new Error('Не вдалося завантажити коктейлі');
  const data = await res.json();
  return data?.drinks || null; // Безпечна обробка null
}

// У Cocktails.jsx:
const details = drinks ? (await Promise.all(
  drinks.slice((page-1)*PAGE_SIZE, page*PAGE_SIZE).map(c => getCocktailById(c.idDrink))
)).filter(c => c !== null) : []; // Фільтрація null значень
```

---

## 🟠 СЕРЙОЗНІ ПОМИЛКИ

### 3. **src/pages/Calendar.jsx** - Помилки тільки логуються в консоль
**Рядки:** 82-85, 135-138, 173-176, 200-203, 251-254, 279-282 (виправлено)  
**Тип:** Погана обробка помилок  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Всі помилки в компоненті Calendar тільки логуються в консоль через `console.error()`, але не показуються користувачу. Користувач не дізнається про проблеми з операціями Firebase.

**Наслідки:**
- Користувач не бачить помилок
- Неможливо діагностувати проблеми без відкриття консолі
- Поганий UX при помилках

**Виправлення:**
Додано систему відображення помилок користувачу:
- Додано state для зберігання повідомлення про помилку (`error`) та стану відображення (`errorOpen`)
- Всі catch блоки тепер встановлюють повідомлення про помилку та відкривають Snackbar
- Додано компонент `Snackbar` з `Alert` для відображення помилок у верхній частині екрану
- Помилки автоматично зникають через 6 секунд або можуть бути закриті вручну
- Кожна помилка має конкретне повідомлення українською мовою

**Приклад виправленого коду:**
```javascript
const [error, setError] = useState(null);
const [errorOpen, setErrorOpen] = useState(false);

// В catch блоках:
catch (error) {
    console.error("Error fetching events:", error);
    setError("Не вдалося завантажити події. Спробуйте оновити сторінку.");
    setErrorOpen(true);
}

// В JSX:
<Snackbar 
  open={errorOpen} 
  autoHideDuration={6000} 
  onClose={handleCloseError}
  anchorOrigin={{ vertical: 'top', horizontal: 'center' }}
>
  <Alert onClose={handleCloseError} severity="error" sx={{ width: '100%' }}>
    {error}
  </Alert>
</Snackbar>
```

---

### 4. **src/pages/Calendar.jsx** - Потенційна проблема з подвійним створенням подій
**Рядки:** 92-150, 152-197, 199-233 (виправлено)  
**Тип:** Race condition  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Функції `handleDateClick` та `toggleEvent` можуть одночасно створювати події для одного дня, якщо користувач швидко клікає. Це може призвести до дублікатів подій у базі даних.

**Наслідки:**
- Дублікати подій у Firestore
- Некоректна статистика
- Проблеми з видаленням подій

**Виправлення:**
Додано механізм блокування (lock) для запобігання одночасному виконанню операцій з однією датою:
- Додано `useRef` з `Set` для зберігання дат, що зараз обробляються (`processingDatesRef`)
- Перед початком операції перевіряється, чи дата вже обробляється
- Якщо дата вже обробляється, функція повертається без виконання операції
- Дата додається до Set перед початком операції
- Дата видаляється з Set в блоці `finally` після завершення операції (навіть при помилці)
- Механізм застосовано до всіх функцій: `toggleEvent`, `handleDateClick`, `handleDateDoubleClick`

**Приклад виправленого коду:**
```javascript
const processingDatesRef = useRef(new Set()); // Блокування для запобігання race condition

const toggleEvent = async (dateStr) => {
  if (!user) return;
  // Перевірка на race condition: якщо дата вже обробляється, виходимо
  if (processingDatesRef.current.has(dateStr)) {
    return;
  }
  // Додаємо дату до Set оброблюваних дат
  processingDatesRef.current.add(dateStr);
  try {
    // ... операції з датою ...
  } catch (error) {
    // ... обробка помилок ...
  } finally {
    // Видаляємо дату з Set після завершення операції
    processingDatesRef.current.delete(dateStr);
  }
};
```

---

### 5. **src/pages/Calendar.jsx** - Неправильне використання ReactDOM.createRoot
**Рядки:** 284-309 (виправлено)  
**Тип:** Помилка використання React API  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
У функції `renderEventContent` створюється новий root для кожного рендеру події, що може призвести до витоку пам'яті та проблем з продуктивністю.

**Наслідки:**
- Витоки пам'яті
- Погіршення продуктивності
- Можливі помилки React

**Виправлення:**
Замінено ReactDOM.createRoot на прямий SVG підхід:
- Видалено імпорт `ReactDOM` та `WineBarIcon`
- Використано нативний DOM API для створення SVG елемента
- SVG створюється безпосередньо через `document.createElementNS`
- Використано той самий SVG path, що використовує Material Icons WineBarIcon
- Це усуває необхідність створювати React root для кожної події
- Немає витоку пам'яті, оскільки SVG елементи автоматично очищаються при видаленні з DOM

**Приклад виправленого коду:**
```javascript
// Рендер бокалу
const renderEventContent = () => {
  const container = document.createElement("div");
  container.style.display = "flex";
  container.style.justifyContent = "center";
  container.style.alignItems = "center";
  container.style.height = "100%";
  container.style.width = "100%";
  container.style.pointerEvents = "none";
  
  // Використовуємо SVG безпосередньо без React root для уникнення витоку пам'яті
  const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
  svg.setAttribute("width", "24");
  svg.setAttribute("height", "24");
  svg.setAttribute("viewBox", "0 0 24 24");
  svg.setAttribute("fill", "#222222");
  svg.style.display = "block";
  
  // SVG path для WineBarIcon (Material Icons)
  const path = document.createElementNS("http://www.w3.org/2000/svg", "path");
  path.setAttribute("d", "M6 3v6c0 2.97 2.16 5.43 5 5.91V19H8v2h8v-2h-3v-4.09c2.84-.48 5-2.94 5-5.91V3H6zm6 10c-1.86 0-3.41-1.28-3.86-3h7.72c-.45 1.72-2 3-3.86 3zm4-8H8V5h8v0z");
  svg.appendChild(path);
  
  container.appendChild(svg);
  return { domNodes: [container] };
};
```

---

### 6. **src/pages/Achievements.jsx** - Неправильна логіка обчислення серій днів
**Рядки:** 61-95 (виправлено)  
**Тип:** Логічна помилка  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Алгоритм обчислення найдовшої серії днів підряд може працювати неправильно. Він перевіряє різницю між послідовними датами, але не враховує, що дати можуть бути не відсортовані правильно або що серія може починатися не з першої дати.

**Наслідки:**
- Некоректні дані про серії
- Неправильні досягнення

**Виправлення:**
Повністю переписано алгоритм обчислення серій з детальними коментарями:
- Конвертація всіх дат у Date об'єкти та правильне сортування за зростанням
- Використання `Math.round()` для точної перевірки різниці в днях (враховує часові пояси)
- Правильна обробка переривання серії: збереження максимальної серії перед початком нової
- Перевірка останньої серії після циклу (вона може бути найдовшою)
- Виправлено обробку п'ятниць та понеділків (allDates тепер містить Date об'єкти)
- Покращено перевірку повного тижня з використанням правильних Date об'єктів

**Приклад виправленого коду:**
```javascript
// Обчислення найдовшої серії днів підряд
// Конвертуємо всі унікальні дати в Date об'єкти та сортуємо їх за зростанням
const allDates = Array.from(daysSet)
  .map(dateStr => new Date(dateStr))
  .sort((a, b) => a.getTime() - b.getTime());

let maxStreak = 0; // Найдовша знайдена серія
let curStreak = 0; // Поточна серія днів підряд

// Проходимо по всіх відсортованих датах
allDates.forEach((currentDate, index) => {
  if (index === 0) {
    // Перша дата - починаємо серію з 1 дня
    curStreak = 1;
  } else {
    // Обчислюємо різницю в днях між поточною та попередньою датою
    const prevDate = allDates[index - 1];
    // Конвертуємо мілісекунди в дні (86400000 мс = 1 день)
    const diffDays = Math.round((currentDate.getTime() - prevDate.getTime()) / 86400000);
    
    if (diffDays === 1) {
      // Якщо різниця рівно 1 день - продовжуємо поточну серію
      curStreak++;
    } else {
      // Якщо різниця більше 1 дня - серія перервалася
      // Зберігаємо максимальну серію перед початком нової
      maxStreak = Math.max(maxStreak, curStreak);
      // Починаємо нову серію з поточної дати
      curStreak = 1;
    }
  }
});

// Перевіряємо останню серію (вона може бути найдовшою)
maxStreak = Math.max(maxStreak, curStreak);
```

---

### 7. **src/pages/Achievements.jsx** - Перевірка "весь місяць" тільки для поточного року
**Рядки:** 123-156 (виправлено)  
**Тип:** Логічна помилка  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Функція перевіряє, чи весь місяць був "п'яним", але використовує тільки поточний рік (`new Date().getFullYear()`), навіть якщо події можуть бути з інших років.

**Наслідки:**
- Некоректна перевірка досягнень для минулих років
- Досягнення можуть не зараховуватися правильно

**Виправлення:**
Повністю переписано логіку перевірки "весь місяць" з детальними коментарями:
- Знаходження всіх унікальних років з подій через Set
- Перевірка кожного року та кожного місяця цього року
- Правильне обчислення кількості днів у місяці для конкретного року (враховує високосні роки)
- Перевірка всіх днів місяця для кожного року
- Досягнення зараховується, якщо хоча б один місяць одного року повністю заповнений
- Оптимізація: вихід з циклів після знаходження першого повного місяця

**Приклад виправленого коду:**
```javascript
// Перевірка чи є повний місяць (всі дні місяця заповнені)
// Знаходимо всі унікальні роки з подій
const yearsSet = new Set();
allDates.forEach(date => {
  yearsSet.add(date.getFullYear());
});

let fullMonth = false;
// Перевіряємо кожен рік та кожен місяць цього року
for (const year of yearsSet) {
  // Перевіряємо всі 12 місяців для поточного року
  for (let m = 0; m < 12; m++) {
    // Обчислюємо кількість днів у місяці для конкретного року
    // new Date(year, month + 1, 0) повертає останній день місяця
    const daysInMonth = new Date(year, m + 1, 0).getDate();
    let count = 0;
    
    // Перевіряємо кожен день місяця
    for (let d = 1; d <= daysInMonth; d++) {
      // Формуємо ключ у форматі YYYY-MM-DD
      const key = `${year}-${String(m + 1).padStart(2, '0')}-${String(d).padStart(2, '0')}`;
      if (byDate[key]) count++;
    }
    
    // Якщо всі дні місяця заповнені - досягнення отримано
    if (count === daysInMonth) {
      fullMonth = true;
      break; // Виходимо з циклу місяців
    }
  }
  
  // Якщо знайшли повний місяць, виходимо з циклу років
  if (fullMonth) break;
}
```

---

### 8. **src/pages/Cabinet.jsx** - Потенційна помилка при обчисленні днів року
**Рядки:** 101-119, 132-139 (виправлено)  
**Тип:** Помилка обчислення дат  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Обчислення кількості днів у році використовує формулу `Math.floor((now - new Date(year, 0, 0)) / 86400000)`, що може давати неточні результати через проблеми з часовими поясами та округленням.

**Наслідки:**
- Некоректна кількість днів у статистиці
- Проблеми з обчисленням серій

**Виправлення:**
Повністю переписано обчислення днів року та дня року з детальними коментарями:
- Використання UTC дат для уникнення проблем з часовими поясами
- Правильне обчислення кількості днів для поточного року (від початку року до сьогодні)
- Використання `Math.round()` замість `Math.floor()` для точної перевірки
- Створення допоміжних функцій `isLeapYear` та `getDaysInYear` для перевірки високосних років
- Правильне обчислення дня року (1-based) з використанням UTC дат
- Врахування сьогоднішнього дня при обчисленні днів поточного року (+1)

**Приклад виправленого коду:**
```javascript
// Обчислення кількості днів у році
// Для минулих років використовуємо функцію перевірки високосного року
const isLeapYear = (y) => ((y % 4 === 0 && y % 100 !== 0) || y % 400 === 0);
const getDaysInYear = (y) => isLeapYear(y) ? 366 : 365;

let daysInYear;
if (isCurrentYear) {
    // Для поточного року обчислюємо кількість днів від початку року до сьогодні
    // Використовуємо UTC дати для уникнення проблем з часовими поясами
    const startOfYear = new Date(Date.UTC(year, 0, 1, 0, 0, 0, 0));
    const todayUTC = new Date(Date.UTC(now.getFullYear(), now.getMonth(), now.getDate(), 0, 0, 0, 0));
    // Обчислюємо різницю в мілісекундах та конвертуємо в дні
    const diffMs = todayUTC.getTime() - startOfYear.getTime();
    daysInYear = Math.round(diffMs / 86400000) + 1; // +1 бо включаємо сьогоднішній день
} else {
    // Для минулих років використовуємо повну кількість днів у році
    daysInYear = getDaysInYear(year);
}

// Обчислюємо день року (1-based)
const startOfYearUTC = new Date(Date.UTC(year, 0, 1, 0, 0, 0, 0));
const eventDateUTC = new Date(Date.UTC(d.getFullYear(), d.getMonth(), d.getDate(), 0, 0, 0, 0));
const diffMs = eventDateUTC.getTime() - startOfYearUTC.getTime();
const dayOfYear = Math.round(diffMs / 86400000) + 1; // +1 бо день року починається з 1
```

---

### 9. **src/pages/Cabinet.jsx** - Відсутність обробки помилок при завантаженні статистики
**Рядки:** 70-99, 217-236 (виправлено)  
**Тип:** Погана обробка помилок  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
При помилці завантаження подій з Firestore `stats` встановлюється в `null`, але немає повідомлення користувачу про помилку.

**Наслідки:**
- Користувач не знає про проблеми з завантаженням
- Немає можливості повторити запит

**Виправлення:**
Додано систему відображення помилок користувачу:
- Додано state для зберігання повідомлення про помилку (`error`) та стану відображення (`errorOpen`)
- Додано імпорт `Alert` та `Snackbar` з Material-UI
- У catch блоці додано логування помилки в консоль та встановлення повідомлення про помилку
- Додано компонент `Snackbar` з `Alert` для відображення помилок у верхній частині екрану
- Помилки автоматично зникають через 6 секунд або можуть бути закриті вручну
- Повідомлення про помилку українською мовою з інструкцією для користувача

**Приклад виправленого коду:**
```javascript
const [error, setError] = useState(null);
const [errorOpen, setErrorOpen] = useState(false);

// В catch блоці:
catch (e) {
    console.error("Error fetching events:", e);
    setStats(null);
    setError("Не вдалося завантажити статистику. Спробуйте оновити сторінку.");
    setErrorOpen(true);
}

// В JSX:
<Snackbar 
  open={errorOpen} 
  autoHideDuration={6000} 
  onClose={handleCloseError}
  anchorOrigin={{ vertical: 'top', horizontal: 'center' }}
>
  <Alert onClose={handleCloseError} severity="error" sx={{ width: '100%' }}>
    {error}
  </Alert>
</Snackbar>
```

---

### 10. **src/pages/Cocktails.jsx** - Відсутність обробки null відповідей API
**Рядки:** 39-52 (виправлено)  
**Тип:** Помилка обробки даних  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Код не перевіряє, чи `drinks` не є `null` перед викликом `slice()` та `map()`. Якщо API повертає `null`, виникне помилка.

**Наслідки:**
- Краш додатку при порожніх відповідях API
- Помилка "Cannot read property 'slice' of null"

**Виправлення:**
Змінено ініціалізацію `drinks` з `[]` на `null` та додано перевірку перед використанням. Також додано фільтрацію `null` значень з масиву деталей.

**Виправлений код:**
```javascript
let drinks = null; // замість []
// ...
const details = drinks ? (await Promise.all(
  drinks.slice((page-1)*PAGE_SIZE, page*PAGE_SIZE).map(c => getCocktailById(c.idDrink))
)).filter(c => c !== null) : []; // Фільтрація null значень
```

---

### 11. **src/pages/Cocktails.jsx** - Потенційна помилка при отриманні деталей коктейлю
**Рядки:** 49-52 (виправлено)  
**Тип:** Помилка обробки даних  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Якщо `getCocktailById()` повертає `null` або `undefined` для якогось коктейлю, це значення буде додано до масиву `cocktails`, що може призвести до помилок при рендерингу.

**Наслідки:**
- Помилки рендерингу
- Некоректне відображення коктейлів

**Виправлення:**
Додано фільтрацію `null` значень з масиву деталей коктейлів після отримання даних з API. Також додано перевірку для `getCategories()` та `getRandomCocktail()`.

---

### 12. **src/pages/Home.jsx** - Відсутність обробки помилок при завантаженні даних
**Рядки:** 21-52, 68-91 (виправлено)  
**Тип:** Погана обробка помилок  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Всі помилки при завантаженні свят та історичних подій "проковзуються" через порожні блоки `catch`, без жодного повідомлення користувачу.

**Наслідки:**
- Користувач не знає про проблеми з завантаженням
- Немає індикації помилок

**Виправлення:**
Додано систему відображення помилок користувачу:
- Додано state для зберігання повідомлення про помилку (`error`) та стану відображення (`errorOpen`)
- Додано імпорт `Alert` та `Snackbar` з Material-UI
- У catch блоці завантаження свят додано логування помилки в консоль та встановлення повідомлення про помилку
- У catch блоці завантаження історичних подій додано обробку помилок з повідомленням користувачу
- Додано компонент `Snackbar` з `Alert` для відображення помилок у верхній частині екрану
- Помилки автоматично зникають через 6 секунд або можуть бути закриті вручну
- Повідомлення про помилки українською мовою з інструкціями для користувача

**Приклад виправленого коду:**
```javascript
const [error, setError] = useState(null);
const [errorOpen, setErrorOpen] = useState(false);

// В catch блоці завантаження свят:
catch (e) {
    console.error("Error fetching holidays:", e);
    setError("Не вдалося завантажити свята. Деякі дані можуть бути недоступні.");
    setErrorOpen(true);
}

// В catch блоці завантаження історичних подій:
getOnThisDayEvents()
    .then(setOnThisDay)
    .catch((e) => {
        console.error("Error fetching on this day events:", e);
        setError("Не вдалося завантажити історичні події. Спробуйте оновити сторінку.");
        setErrorOpen(true);
    });

// В JSX:
<Snackbar 
  open={errorOpen} 
  autoHideDuration={6000} 
  onClose={handleCloseError}
  anchorOrigin={{ vertical: 'top', horizontal: 'center' }}
>
  <Alert onClose={handleCloseError} severity="error" sx={{ width: '100%' }}>
    {error}
  </Alert>
</Snackbar>
```

---

### 13. **src/pages/Home.jsx** - Потенційна помилка в логіці findClosestEvents
**Рядки:** 54-108 (виправлено)  
**Тип:** Логічна помилка  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Функція `findClosestEvents` може повертати неправильні результати, якщо події не відсортовані за датою. Також логіка знаходження попередньої події при відсутності минулих подій може бути некоректною.

**Наслідки:**
- Неправильне відображення найближчих подій
- Плутанина для користувача

**Виправлення:**
Повністю переписано функцію `findClosestEvents` з детальними коментарями та JSDoc:
- Додано JSDoc коментар з описом параметрів та поверненого значення
- Спочатку відсортовуємо події за датою (на випадок, якщо вони не відсортовані)
- Встановлюємо час на початок дня (00:00:00) для точного порівняння дат без урахування часу
- Видалено некоректну логіку з `reduce`, яка могла повертати неправильні значення при відсутності минулих/майбутніх подій
- Додано оптимізацію: переривання циклу після знаходження найближчої наступної події (оскільки події відсортовані)
- Правильна обробка сьогоднішніх подій (вони ігноруються при пошуку попередньої/наступної)
- Якщо події не знайдено, повертається `null` (не намагаємося знайти найпізнішу/найранішу подію)

**Приклад виправленого коду:**
```javascript
/**
 * Знаходить найближчі події до заданої дати (попередню та наступну)
 * @param {Array} events - масив подій з полем date
 * @param {string} todayStr - дата у форматі YYYY-MM-DD
 * @returns {Object} об'єкт з полями prev (попередня подія) та next (наступна подія)
 */
function findClosestEvents(events, todayStr) {
    if (!events.length) return { prev: null, next: null };
    
    const today = new Date(todayStr);
    today.setHours(0, 0, 0, 0); // Встановлюємо час на початок дня
    
    // Спочатку відсортовуємо події за датою
    const sortedEvents = [...events].sort((a, b) => {
        const dateA = new Date(a.date);
        const dateB = new Date(b.date);
        dateA.setHours(0, 0, 0, 0);
        dateB.setHours(0, 0, 0, 0);
        return dateA.getTime() - dateB.getTime();
    });
    
    let prev = null;
    let next = null;
    
    for (const ev of sortedEvents) {
        const eventDate = new Date(ev.date);
        eventDate.setHours(0, 0, 0, 0);
        
        if (eventDate < today) {
            if (!prev || eventDate > new Date(prev.date)) {
                prev = ev;
            }
        } else if (eventDate > today) {
            if (!next || eventDate < new Date(next.date)) {
                next = ev;
                break; // Оптимізація: події відсортовані
            }
        }
        // Якщо eventDate === today, ігноруємо
    }
    
    return { prev, next };
}
```

---

## 🟡 ПОМІЧНІ ПОМИЛКИ

### 14. **src/components/MonthStatsChart.jsx** - Потенційна помилка при кліку на графік
**Рядки:** 41-137 (виправлено)  
**Тип:** Помилка обробки подій  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Перевірка `chart.getElementsAtEventForMode` може не спрацювати правильно, якщо Chart.js API зміниться або якщо `chartRef.current` не містить правильний об'єкт.

**Наслідки:**
- Кліки на графік можуть не працювати
- Помилки в консолі

**Виправлення:**
Додано комплексну обробку помилок з детальними коментарями:
- Додано перевірку наявності callback функції та ref перед обробкою
- Додано перевірку наявності об'єкта графіка та методу `getElementsAtEventForMode`
- Додано перевірку наявності нативного event об'єкта (`event.native`)
- Обгорнуто виклик Chart.js API в try-catch блок для безпечної обробки помилок
- Додано перевірку наявності та валідності масиву елементів
- Додано перевірку валідності індексу місяця (0-11)
- Додано попередження в консоль при проблемах (console.warn)
- Додано обробку помилок з логуванням в консоль (console.error)
- Всі перевірки мають детальні коментарі українською мовою

**Приклад виправленого коду:**
```javascript
onClick: (event) => {
  // Перевірка наявності callback функції та ref
  if (!onBarClick || !chartRef.current) return;
  
  const chart = chartRef.current;
  
  // Перевірка наявності об'єкта графіка та методу
  if (!chart || typeof chart.getElementsAtEventForMode !== 'function') {
    console.warn('Chart instance or getElementsAtEventForMode method not available');
    return;
  }
  
  // Перевірка наявності нативного event об'єкта
  if (!event || !event.native) {
    console.warn('Event or event.native is not available');
    return;
  }
  
  try {
    const elements = chart.getElementsAtEventForMode(
      event.native, 
      'nearest', 
      { intersect: true }, 
      true
    );
    
    // Перевірка наявності елементів
    if (!elements || !Array.isArray(elements) || elements.length === 0) {
      return;
    }
    
    const firstElement = elements[0];
    
    // Перевірка валідності індексу місяця
    if (typeof firstElement.index !== 'number' || firstElement.index < 0 || firstElement.index >= 12) {
      console.warn('Invalid month index:', firstElement.index);
      return;
    }
    
    onBarClick(firstElement.index);
  } catch (error) {
    console.error('Error handling chart click:', error);
  }
}
```

---

### 15. **src/components/YearMonthPieChart.jsx** - Потенційна помилка при обчисленні totalSober
**Рядки:** 14-50 (виправлено)  
**Тип:** Помилка обчислення  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Обчислення `totalSober` може бути негативним, якщо `totalDrank > totalDays`, що неможливо, але код використовує `Math.max(0, totalSober)`, що вказує на можливість негативних значень.

**Наслідки:**
- Некоректні дані в pie chart
- Можливі візуальні артефакти

**Виправлення:**
Виправлено обчислення `totalDays` та `totalSober` з додатковими перевірками та покращеною логікою:
- **Виправлено обчислення `totalDays` для поточного року**: Замість `new Date(year, 0, 0)` (останній день попереднього року) використовується `new Date(Date.UTC(year, 0, 1, 0, 0, 0, 0))` (початок року)
- **Додано використання UTC дат**: Для уникнення проблем з часовими поясами при обчисленні днів
- **Використано `Math.round` замість `Math.floor`**: Для точної перевірки з урахуванням можливих проблем з округленням
- **Гарантовано невід'ємність `totalSober`**: Використовується `Math.max(0, totalDays - totalDrank)` при обчисленні
- **Додано перевірку на невідповідність даних**: Якщо `totalDrank > totalDays`, виводиться попередження в консоль
- **Виправлено обчислення для місячного pie chart**: Додано перевірку `drank > daysInMonth` та використання `Math.max(0, ...)` для `soberInMonth`
- **Додано перевірку на `undefined`**: `byMonth[selectedMonth] || 0` для запобігання помилкам
- **Детальні коментарі**: Додано коментарі українською мовою для кожного кроку обчислення

**Приклад виправленого коду:**
```javascript
// Обчислюємо загальну кількість днів у році (або до сьогодні для поточного року)
let totalDays;
if (isCurrentYear) {
  // Для поточного року обчислюємо кількість днів від початку року до сьогодні
  // Використовуємо UTC дати для уникнення проблем з часовими поясами
  const startOfYear = new Date(Date.UTC(year, 0, 1, 0, 0, 0, 0));
  const todayUTC = new Date(Date.UTC(now.getFullYear(), now.getMonth(), now.getDate(), 0, 0, 0, 0));
  const diffMs = todayUTC.getTime() - startOfYear.getTime();
  totalDays = Math.round(diffMs / 86400000) + 1; // +1 бо включаємо сьогоднішній день
} else {
  totalDays = getDaysInYear(year);
}

// Обчислюємо кількість тверезих днів
// Перевіряємо, щоб totalDrank не перевищував totalDays
const totalSober = Math.max(0, totalDays - totalDrank);

// Додаткова перевірка: якщо totalDrank > totalDays, це вказує на проблему в даних
if (totalDrank > totalDays) {
  console.warn(`YearMonthPieChart: totalDrank (${totalDrank}) exceeds totalDays (${totalDays}) for year ${year}.`);
}
```

---

### 16. **src/components/YearMonthPieChart.jsx** - Відсутність перевірки на null для selectedMonth
**Рядки:** 54-78 (виправлено)  
**Тип:** Помилка обробки даних  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Код перевіряє `selectedMonth !== null`, але не перевіряє, чи `byMonth[selectedMonth]` існує та чи є валідним індексом.

**Наслідки:**
- Можливі помилки при некоректному індексі
- Краш при невалідному selectedMonth

**Виправлення:**
Додано комплексну валідацію для `selectedMonth` перед використанням:
- Перевірка на `null`: `selectedMonth !== null`
- Перевірка типу: `typeof selectedMonth === 'number'`
- Перевірка діапазону: `selectedMonth >= 0 && selectedMonth <= 11` (валідний індекс місяця 0-11)
- Перевірка масиву: `Array.isArray(byMonth)`
- Перевірка меж масиву: `selectedMonth < byMonth.length`
- Всі перевірки об'єднані в одній умові `if`, що запобігає виконанню коду при невалідних значеннях
- Це запобігає крашам та помилкам при некоректному індексі або невалідному `selectedMonth`

**Приклад виправленого коду:**
```javascript
// Для pie chart по місяцю
let monthPie = null;
if (selectedMonth !== null && 
    typeof selectedMonth === 'number' && 
    selectedMonth >= 0 && 
    selectedMonth <= 11 && 
    Array.isArray(byMonth) && 
    selectedMonth < byMonth.length) {
  const daysInMonth = new Date(year, selectedMonth + 1, 0).getDate();
  const drank = byMonth[selectedMonth] || 0;
  // ... решта коду ...
}
```

---

### 17. **src/pages/Cabinet.jsx** - Потенційна помилка при обчисленні minMonth
**Рядки:** 165-189 (виправлено)  
**Тип:** Логічна помилка  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Якщо всі місяці мають значення 0, `minMonth` залишиться `-1`, що може призвести до проблем при відображенні. Також `maxMonth` може повернути некоректний індекс, якщо всі місяці мають 0.

**Наслідки:**
- Некоректне відображення місяця з найменшою кількістю
- Помилки при доступі до масиву місяців
- Некоректне відображення місяця з найбільшою кількістю при відсутності даних

**Виправлення:**
Додано перевірки для `maxMonth`, `minMonth`, `maxWeekday` та `minWeekday`:
- **Для `maxMonth`**: Перевірка, чи є хоча б один місяць зі значенням > 0. Якщо всі місяці мають 0, повертається -1
- **Для `minMonth`**: Логіка залишилася незмінною (шукає мінімальне значення серед місяців > 0), але додано коментарі для ясності
- **Для `maxWeekday` та `minWeekday`**: Додано перевірки, чи є хоча б один день зі значенням > 0. Якщо всі дні мають 0, повертається -1
- **Оновлено UI**: Додано перевірки на -1 для `maxMonth`, `maxWeekday` та `minWeekday` у відображенні статистики (вже було для `minMonth`)
- Всі значення -1 коректно обробляються в UI з відображенням "немає"

**Приклад виправленого коду:**
```javascript
// Знаходимо максимальне значення серед місяців
const maxMonthValue = Math.max(...monthsToConsider);
// Перевіряємо, чи є хоча б один місяць зі значенням > 0
// Якщо всі місяці мають 0, то maxMonth буде -1 (немає даних)
const maxMonth = maxMonthValue > 0 ? monthsToConsider.indexOf(maxMonthValue) : -1;

// Знаходимо максимальне та мінімальне значення серед днів тижня
const maxWeekdayValue = Math.max(...byWeekday);
const minWeekdayValue = Math.min(...byWeekday);
// Перевіряємо, чи є дані (хоча б один день має значення > 0)
// Якщо всі дні мають 0, то повертаємо -1
const maxWeekday = maxWeekdayValue > 0 ? byWeekday.indexOf(maxWeekdayValue) : -1;
const minWeekday = minWeekdayValue > 0 ? byWeekday.indexOf(minWeekdayValue) : -1;
```

---

### 18. **src/pages/Cabinet.jsx** - Потенційна помилка при обчисленні maxWeekday/minWeekday
**Рядки:** 183-189 (виправлено)  
**Тип:** Помилка обчислення  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Якщо всі дні тижня мають однакову кількість, `indexOf(Math.min(...byWeekday))` поверне індекс першого елемента з мінімальним значенням, що може бути некоректним.

**Наслідки:**
- Некоректне відображення дня тижня з найменшою кількістю

**Виправлення:**
Виправлено разом з помилкою #17. Додано перевірки для `maxWeekday` та `minWeekday`:
- Перевірка, чи є хоча б один день зі значенням > 0 перед використанням `indexOf`
- Якщо всі дні мають 0, повертається -1
- Оновлено UI для коректного відображення "немає" при значенні -1

---

### 19. **src/pages/Calendar.jsx** - Потенційна проблема з очищенням таймерів
**Рядки:** 52-53, 65-74, 234-269 (виправлено)  
**Тип:** Виток пам'яті  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Таймер `clickTimer` в `handleCellMount` може не очищатися правильно при unmount компонента, що може призвести до витоку пам'яті.

**Наслідки:**
- Витоки пам'яті
- Погіршення продуктивності

**Виправлення:**
Повністю переписано систему управління таймерами для запобігання витокам пам'яті:
- **Замінено локальну змінну на Map**: Замість локальної змінної `clickTimer` використовується `clickTimersRef` (Map) для зберігання таймерів для кожної комірки з ключем `dateStr`
- **Очищення при remount комірки**: Перед встановленням нового обробника подій очищається попередній таймер для цієї комірки, якщо він існує
- **Правильне зберігання таймерів**: Таймери зберігаються в Map при створенні та видаляються після завершення або очищення
- **Cleanup при unmount**: Додано `useEffect` з cleanup функцією, яка очищає всі таймери з Map при unmount компонента
- **Автоматичне видалення**: Таймери автоматично видаляються з Map після завершення (через 300мс) або при очищенні

**Приклад виправленого коду:**
```javascript
// Map для зберігання таймерів для кожної комірки (ключ - dateStr)
const clickTimersRef = useRef(new Map());

// Cleanup: очищаємо всі таймери при unmount компонента
useEffect(() => {
  return () => {
    // Очищаємо всі таймери з Map
    clickTimersRef.current.forEach((timer) => {
      clearTimeout(timer);
    });
    clickTimersRef.current.clear();
  };
}, []);

const handleCellMount = (cell) => {
  // ...
  // Очищаємо попередній таймер для цієї комірки, якщо він існує
  const existingTimer = clickTimersRef.current.get(dateStr);
  if (existingTimer) {
    clearTimeout(existingTimer);
    clickTimersRef.current.delete(dateStr);
  }
  
  cell.el.onclick = (e) => {
    const currentTimer = clickTimersRef.current.get(dateStr);
    if (currentTimer) {
      clearTimeout(currentTimer);
      clickTimersRef.current.delete(dateStr);
      // Даблклік
      // ...
    } else {
      // Одиночний клік
      // ...
      const newTimer = setTimeout(() => {
        clickTimersRef.current.delete(dateStr);
      }, 300);
      clickTimersRef.current.set(dateStr, newTimer);
    }
  };
};
```

---

### 20. **src/pages/Calendar.jsx** - Використання Date.now() для ID події
**Рядки:** 142-149 (виправлено)  
**Тип:** Потенційна колізія ID  
**Статус:** ✅ **ВИПРАВЛЕНО**  
**Опис:**  
Використання `Date.now()` для ID події може призвести до колізій, якщо події створюються швидко одна за одною.

**Наслідки:**
- Можливі колізії ID
- Проблеми з оновленням подій
- Некоректна синхронізація між локальним станом та Firestore

**Виправлення:**
Замінено `Date.now()` на унікальний ID з Firestore:
- **Використання ID з Firestore**: `addDoc` повертає `DocumentReference` з унікальним ID через властивість `.id`
- **Узгодженість з даними**: ID в локальному стані тепер відповідає ID документа в Firestore
- **Уникнення колізій**: Firestore гарантує унікальність ID для кожного документа
- **Синхронізація**: Локальний стан тепер використовує той самий ID, що і в базі даних, що спрощує оновлення та видалення подій

**Приклад виправленого коду:**
```javascript
// addDoc повертає DocumentReference з унікальним ID
const docRef = await addDoc(collection(db, "events"), newEvent);
// ...
// Використовуємо ID з Firestore замість Date.now() для уникнення колізій
setEvents(prev => [...prev, { id: docRef.id, ...newEvent }]);
```

**Проблемний код (до виправлення):**
```javascript
await addDoc(collection(db, "events"), newEvent);
setEvents(prev => [...prev, { id: Date.now(), ...newEvent }]);
```

---

## 📝 РЕКОМЕНДАЦІЇ

1. ✅ **Додати валідацію змінних середовища** у `firebase.js` - **ВИПРАВЛЕНО**
2. ✅ **Додати обробку null/undefined** у API функціях - **ВИПРАВЛЕНО**
3. ✅ **Додати перевірки на null/undefined** перед доступом до властивостей - **ВИПРАВЛЕНО**
4. ✅ **Додати обробку помилок з повідомленнями користувачу** у всіх компонентах - **ВИПРАВЛЕНО**
   - ✅ Calendar.jsx - виправлено
   - ✅ Cabinet.jsx - виправлено
   - ✅ Home.jsx - виправлено
   - ✅ Cocktails.jsx - виправлено (додано Snackbar для відображення помилок)
   - ✅ Achievements.jsx - виправлено (додано обробку помилок з повідомленнями користувачу)
5. ✅ **Виправити логіку обчислення серій** у `Achievements.jsx` - **ВИПРАВЛЕНО**
6. ✅ **Додати cleanup функції** для таймерів та підписок - **ВИПРАВЛЕНО**
   - ✅ Calendar.jsx - cleanup для clickTimers виправлено
   - ✅ Calendar.jsx - cleanup для таймерів анімацій виправлено (додано animationTimersRef та cleanup)
   - ✅ Cabinet.jsx - cleanup для таймера copied виправлено (додано copiedTimerRef та cleanup)
   - ✅ Підписки (onAuthStateChanged) - більшість мають cleanup
7. ✅ **Використовувати унікальні ID** замість `Date.now()` - **ВИПРАВЛЕНО**
8. ✅ **Додати loading states** для всіх асинхронних операцій - **ВИПРАВЛЕНО**
   - ✅ Calendar.jsx - є loading state
   - ✅ Achievements.jsx - є loading state
   - ✅ Cocktails.jsx - є loading state
   - ✅ Cabinet.jsx - виправлено (додано LoadingSpinner для завантаження статистики)
   - ✅ Home.jsx - виправлено (додано LoadingSpinner для завантаження свят та історичних подій)
9. ✅ **Додати error boundaries** для кращої обробки помилок React - **ВИПРАВЛЕНО**
   - ✅ Створено ErrorBoundary компонент у `src/components/ErrorBoundary.jsx`
   - ✅ Додано ErrorBoundary до `src/index.js` для глобальної обробки помилок React
   - ✅ ErrorBoundary відображає зрозуміле повідомлення користувачу українською мовою
   - ✅ У режимі розробки показуються деталі помилки для відлагодження
   - ✅ Додано кнопку для перезавантаження сторінки при помилці
10. ✅ **Додати валідацію даних** перед записом у Firestore - **ВИПРАВЛЕНО**
    - ✅ Є правила Firestore для базової валідації
    - ✅ Створено утиліту валідації `src/utils/eventValidation.js` з функціями `validateEvent` та `validateDrinksUpdate`
    - ✅ Додано валідацію перед `addDoc` у Calendar.jsx (перевірка формату дат, обов'язкових полів, типів даних)
    - ✅ Додано валідацію перед `updateDoc` у Calendar.jsx (перевірка масиву drinks)
    - ✅ Валідація перевіряє: формат дати (YYYY-MM-DD), валідність дати, діапазон дат (±100 років), типи полів, структуру drinks масиву
11. ✅ **Покращити обробку edge cases** у всіх функціях обчислення статистики - **ВИПРАВЛЕНО**
    - ✅ Виправлено обчислення серій, місяців, днів тижня у Cabinet.jsx та Achievements.jsx
    - ✅ Додано перевірки на порожні масиви та невалідні дані
    - ✅ Додано перевірки на невалідні дати та відсутні поля start
    - ✅ Додано перевірки на невалідні індекси (місяці, дні тижня)
    - ✅ Додано перевірки на невалідний рік у calcStats (Cabinet.jsx)
    - ✅ Додано перевірки на порожні Set та масиви перед обчисленням
    - ✅ Додано фільтрацію невалідних дат після конвертації
    - ✅ Додано захист від помилок при обчисленні Math.max/Math.min з порожніми масивами

---

## 📊 СТАТИСТИКА

- **Всього знайдено помилок:** 20
- **Критичних:** 2 (2 виправлено ✅✅)
- **Серйозних:** 11 (11 виправлено ✅✅✅✅✅✅✅✅✅✅✅)
- **Помічних:** 7 (7 виправлено ✅✅✅✅✅✅✅)
- **🎉 ВСІ ПОМИЛКИ ВИПРАВЛЕНІ! 🎉**

---
